[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x"]

[phases.install]
cmds = [
  "npm install",
  "npx playwright install chromium",
  "npx playwright install-deps chromium || true"
]

[start]
cmd = "npm start"
